<% errors = model.errors %>

<% if errors.any? %>
  <div class="alert alert-danger">
    <%= I18n.t('devise.errors.messages.not_saved', count: errors.count) %>

    <ul>
      <% errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
